package tests.flows;

import core.base.BaseTest;
import flows.credit.ProductSelectionFlow;
import org.testng.annotations.Test;

/*public class ProductSelectionFlowTest extends BaseTest {

    @Test
    public void shouldSelectProductByCriteria() {

        // arrange
        ProductSelectionFlow productFlow =
                new ProductSelectionFlow(ctx);

        // ⚠️ ПРЕДУСЛОВИЕ:
        // Здесь тест должен запускаться ПОСЛЕ:
        // - логина
        // - выбора рабочего места
        // - открытия формы подбора продукта
        //
        // ВАЖНО: если этого нет — тест упадёт корректно

        // act
        productFlow.selectProduct(
                "Карзхои гуногунмаксад",
                "Барои эхтиёчоти оилави",
                "50000",
                "36",
                "Сомони Чумхурии Точикистон"
        );

        // assert (минимальный)
        // Например: кнопка "Выбрать" исчезла
        // или открылся следующий экран
        // (можно добавить позже)
    }
}*/
