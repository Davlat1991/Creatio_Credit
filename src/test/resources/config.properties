##########################################
#     LOCAL OVERRIDES / SECRETS ONLY
#     (?? environment, ?? base.url)
##########################################

# ===============================
# Database (optional, local only)
# ===============================
db.url=jdbc:postgresql://<host>:5432/<db>
db.user=<USER>
db.password=<PASSWORD>

# ===============================
# Users (local overrides, optional)
# ===============================
user.davlat.login=<LOGIN>
user.davlat.password=<PASSWORD>

user.admin.login=<LOGIN>
user.admin.password=<PASSWORD>

# ===============================
# Optional local flags (future)
# ===============================
# retry.count=2
# allure.owner=Davlat
# debug.enabled=true
