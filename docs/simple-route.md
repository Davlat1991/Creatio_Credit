# 💳 Creatio Credit — Упрощённый маршрут (UI Automation Documentation)

Документация описывает архитектуру, логику и тестовое покрытие **Упрощённого кредитного маршрута** в рамках UI-автоматизации Creatio.

Маршрут включает:
- Smoke-сценарий (базовый happy path)
- Негативные сценарии (обязательные поля, ошибки)
- Пограничные сценарии (boundary values)
- Стандарты Allure-меток
- Порядок запуска и расширения покрытия

Документ предназначен для QA Automation, QA Manual и разработчиков платформенной команды Creatio.

---

# 🧱 1. Архитектура тестов упрощённого маршрута

Структура каталога:


PageObject'ы маршрута:

| Класс | Назначение |
|------|------------|
| `SimpleRoutePage` | Основная мини-страница заявки |
| `DashboardPage` | Навигация по рабочим местам |
| `WorkspaceSteps` | Переход в разделы Creatio |
| `LoginPage` | Авторизация |

---

# 🚦 2. Smoke-сценарий упрощённого маршрута

Цель: проверить основной пользовательский поток.

**Flow:**
1. Авторизация пользователем USER_DAVLAT
2. Открытие рабочего места **"Розничный менеджер"**
3. Переход в раздел **"Заявки"**
4. Создание новой заявки
5. Заполнение обязательных полей
6. Сохранение
7. Проверка статуса **"Создано"**




### Пример теста:

```java
@Test(description = "Smoke: создание заявки")
public void createSimpleRouteApplication() {

    loginPage.openLoginPage()
             .loginAs(Environment.USER_DAVLAT);

    workspaceSteps.openWorkspaceAndSection("Розничный менеджер", "Заявки");
    dashboardPage.openCreateMenu();

    new SimpleRoutePage()
            .waitOpened()
            .fillRequiredFields("Тестовый Клиент", "9000000000")
            .save()
            .verifyStatus("Создано");
           
}

```

# 🚦 ❌ 3. Негативные сценарии

Цель: убедиться, что Creatio корректно валидирует обязательные поля и ошибки.

3.1 Пустые обязательные поля

Ожидание:

"Заполните обязательные поля"

Пример теста:

```java

@Test(description = "Negative: пустые обязательные поля")
public void emptyRequiredFieldsTest() {

    loginPage.openLoginPage()
             .loginAs(Environment.USER_DAVLAT);

    workspaceSteps.openWorkspaceAndSection("Розничный менеджер", "Заявки");
    dashboardPage.openCreateMenu();

    new SimpleRoutePage()
            .waitOpened()
            .save()
            .shouldHaveError("Заполните обязательные поля");
}

```


# 4. Boundary-сценарии (пограничные значения)

Creatio должна корректно обрабатывать крайние значения полей.

4.1 ФИО

| Значение | Ожидаемое поведение             |
| -------- | ------------------------------- |
| `"А"`    | Ошибка (слишком коротко)        |
| `"Ан"`   | Допустимое минимальное значение |


Invalid:
```java
@Test(description = "Boundary: слишком короткое ФИО")
public void fioTooShortTest() {
...
new SimpleRoutePage()
.fillRequiredFields("А", "9000000000")
.save()
.shouldHaveError("Введите корректное ФИО");
}

```
Valid:

```java
@Test(description = "Boundary: минимально допустимое ФИО")
public void fioValidMinLengthTest() {
...
new SimpleRoutePage()
.fillRequiredFields("Ан", "9000000000")
.save()
.verifyStatus("Создано");
}

```

# 5. Allure-метки (стандарт проекта)

Используется единый корпоративный стиль:

Epic
```kotlin
@Epic("Creatio Credit")
```
Feature
```kotlin
@Feature("Упрощённый маршрут")
```
Story
```kotlin
@Story("Negative: обязательные поля")
```
Owner
```kotlin
@Owner("Davlat")
```
Severity

| Тип теста | Значение |
| --------- | -------- |
| Smoke     | CRITICAL |
| Negative  | NORMAL   |
| Boundary  | MINOR    |


# 6. Запуск тестов
Все тесты маршрута:

```bash
mvn clean test -Dgroups=credit
```
Только smoke:
```bash
mvn test -Dgroups=smoke
```
Только negative:
```bash
mvn test -Dgroups=negative
```
Только boundary:
```bash
mvn test -Dgroups=boundary
```


🤝 Ответственный

Davlat — QA Automation Engineer

Проект: Creatio Credit UI Automation

Команда: Platform Creatio

